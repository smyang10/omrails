<%= form_for(deck) do |f| %>
  <% if deck.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(deck.errors.count, "error") %> prohibited this deck from being saved:</h2>

      <ul>
      <% deck.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<!--
  <div class="form-group">
    <%= f.label :user_id %>
    <%= f.text_field :user_id %>
  </div>
-->
  <div class="form-group">
    <%= f.label :cards %>
    <%= f.text_area :cards, class: "form-control card-list" %>
  </div>


  <div class="form-group">
    <%= f.label :size %>
    <%= f.number_field :size, class: "form-control deck-size" %>
  </div>


  <div class="container container-cards">
    <div class="row">
      <div class="col-md-12">
        <div class="card">
            <%= image_tag("finkle_einhorn.png", class: "card-img") %>
            <%= image_tag("northshire_cleric.png", class: "card-img") %>
            <%= image_tag("firelands_portal.png", class: "card-img") %>
            <%= image_tag("leeroy_jenkins.png", class: "card-img") %>
            <%= image_tag("ragnaros.png", class: "card-img") %>
            <%= image_tag("reno_jackson.png", class: "card-img") %>
        </div>
      </div>
    </div>
  </div>

  <%= f.submit class: "btn btn-primary"%>
<% end %>
